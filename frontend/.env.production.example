# Copy to `.env.production` (gitignored), then: npm run build && npm run webflow:bundle
# No trailing slash. Use your live Render URL so the Designer extension calls your API.
VITE_API_BASE_URL=https://icondock.onrender.com
