# When the UI runs inside the Webflow Designer extension iframe, requests cannot use relative /api paths.
# Set this to your deployed IconDock API origin (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://your-api.example.com
#
# Local `webflow extension serve` (often http://localhost:1337): if unset, the app defaults API calls to
# http://localhost:8787 — keep the backend running there, or set VITE_API_BASE_URL explicitly.
